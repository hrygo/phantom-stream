# [红队] 攻防行动报告: Phase 3+ (Zlib Aware)

**日期**: 2025-12-04
**操作员**: Attacker (红队)

## 1. 核心结论 (Conclusion)
攻方成功识别并移除了伪装成 PDF 增量更新的隐写数据。尽管样本名称暗示了 "Zlib Aware"，但其核心结构仍为增量更新（Revision），因此红队的版本回滚策略依然有效。

## 2. 样本交付 (Sample Delivery)
**关键文件清单**:
*   **输入样本 (Input)**: `docs/2511.17467v2_phase2_zlib_aware.pdf`
*   **输出样本 (Output)**: `docs/2511.17467v2_phase2_zlib_aware_rollback.pdf`

## 3. 技术对抗 (Technical Confrontation)
*   **观测到的战术**:
    *   **结构**: 文件包含多个 `%%EOF` 标记，表明使用了增量更新。
    *   **异常**: `Scan` 工具同时报告了 "Internal Gaps" (1517 bytes)，这可能意味着 Defender 在增量更新包内部还使用了间隙注入，或者增量更新本身被误报为间隙。
*   **采取的措施**: 优先使用 `Rollback` (版本回滚) 工具。
    *   **决策逻辑**: 在 PDF 取证中，"移除不明修订" 的优先级高于 "清洗内部间隙"。如果整个修订版本都是恶意的，直接回滚是最彻底的清洗方式。
*   **技术原理**:
    > 强制将文件物理截断至倒数第二个版本结束处，从而彻底剥离最后一次包含隐写数据的更新。

## 4. 下一步计划 (Next Steps)
红队已完成对该样本的处置。等待蓝队验证清洗效果。
